server:
  port: 8080

app:
  math-service:
    host: nginx
    port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  endpoint:
    prometheus:
      enabled: true

---
spring.config.activate.on-profile: docker

server:
  port: 8080