services:
  publisher:
    build: publisher-service
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    depends_on:
      mosquitto:
        condition: service_healthy

  subscriber-1:
    build: subscriber-service
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - MQTT_URL=tcp://mosquitto:1883
      - MQTT_USERNAME=
      - MQTT_PASSWORD=
      - MQTT_CLEAN_SESSION=true
      - MQTT_CLIENT_ID=subscriber-service
      - MQTT_DEFAULT_TOPIC=bank/#
    depends_on:
      mosquitto:
        condition: service_healthy

  mosquitto:
    image: eclipse-mosquitto
    ports:
      - "1883:1883"     # MQTT broker port
    volumes:
      - ./mosquitto/config:/mosquitto/config
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/log:/mosquitto/log
    healthcheck:
      test: ["CMD-SHELL", "timeout 1 mosquitto_sub -h localhost -t healthcheck -C 1 || exit 1"]
      interval: 5s
      timeout: 2s
      retries: 60
